EbRPG.SCALE_MODE = {
	NORMAL: 0,
	NEAREST: 1,
	HYBRID: 2,
};

EbRPG.settings = {
	width: 320,
	height: 240,
	tileSize: 16,
	cellSize: 24,
	scaleMode: EbRPG.SCALE_MODE.HYBRID,
	maintainAspectRatio: true,
};

EbRPG.configure=(options)=>{

};